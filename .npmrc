# Package settings
package-lock=false

# Ensure the package is published as public (not scoped/private)
access=public

# Save exact versions (optional, but recommended for stability)
save-exact=false

